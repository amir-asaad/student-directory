import React from 'react';

const RemoveStudent = ({ handleRemoveStudentButton }) => (
  <div>
    <button onClick={handleRemoveStudentButton}>Remove Student</button>
  </div>
);

export default RemoveStudent;